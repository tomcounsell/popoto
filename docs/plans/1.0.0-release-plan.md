# Popoto 1.0.0 Release Plan

## Overview

Comprehensive production-ready checklist based on full codebase audit. Current version: 0.9.0.

---

## Phase 1: CRITICAL (Release Blockers)

### 1.1 Connection Handling Hardening
**File:** `src/popoto/redis_db.py`

| Task | Lines | Effort |
|------|-------|--------|
| Add socket timeouts to Redis connection (socket_timeout=5, socket_connect_timeout=5) | 75-80 | S |
| Fix silent failure: re-raise exception after logging instead of continuing | 83-84 | S |
| Add `check_connection() -> bool` health check function | new | S |

### 1.2 Fix Bare Except
**File:** `src/popoto/models/base.py`

| Task | Lines | Effort |
|------|-------|--------|
| Replace bare `except:` with `except Exception:` in `__eq__` method | ~708 | S |

### 1.3 Thread Safety Documentation
**File:** `docs/configuration.md`

| Task | Effort |
|------|--------|
| Add "Thread Safety" section: Redis connections are thread-safe, model instances are not | S |

---

## Phase 2: HIGH (Should Fix)

### 2.1 Export Exceptions
**File:** `src/popoto/__init__.py`

| Task | Effort |
|------|--------|
| Import and add to `__all__`: ModelException, QueryException, PublisherException, SubscriberException | S |
| Remove duplicate ModelException from models/base.py (keep exceptions.py version) | S |

### 2.2 Consolidate Version
| File | Task | Effort |
|------|------|--------|
| `pyproject.toml:7` | Update to `version = "1.0.0"` (single source of truth) | S |
| `pyproject.toml:53` | Remove duplicate version from `[tool.poetry]` section | S |
| `setup.cfg:3` | Remove version line (let pyproject.toml be authoritative) | S |

### 2.3 Add Core Type Hints
**File:** `src/popoto/models/base.py`

| Method | Line | Return Type |
|--------|------|-------------|
| `save()` | 939 | `-> Union[Pipeline, int, bool]` |
| `delete()` | 1189 | `-> Union[Pipeline, bool]` |
| `create()` | 1144 | `-> Union[Pipeline, Model]` |
| `load()` | 1166 | `-> Optional[Model]` |

**File:** `src/popoto/models/query.py`

| Method | Line | Return Type |
|--------|------|-------------|
| `filter()` | 462 | `-> List[Model]` |
| `get()` | ~400 | `-> Model` |

### 2.4 Create CHANGELOG.md
**File:** `CHANGELOG.md` (new)

| Task | Effort |
|------|--------|
| Create changelog following Keep a Changelog format | M |
| Document 0.9.0 → 1.0.0 changes (connection hardening, exception exports, type hints) | M |

---

## Phase 3: MEDIUM (Nice to Have)

### 3.1 Add Connection Tests
**File:** `tests/test_connection.py` (new)

- Test graceful failure when Redis unavailable
- Test reconnection behavior

### 3.2 Add Concurrent Operation Tests
**File:** `tests/test_stress.py`

- Test concurrent saves to same key from multiple threads
- Document expected last-write-wins behavior

### 3.3 Logging Configuration
**File:** `src/popoto/redis_db.py`

- Add `POPOTO_LOG_LEVEL` env var support
- Document in `docs/configuration.md`

---

## Defer to 1.1.0

| Item | Reason |
|------|--------|
| Key partitioning (redis_db.py:108 TODO) | Performance optimization |
| Complete API reference docs | Existing docs comprehensive |
| Remaining type hint coverage (~45%) | Core methods sufficient |
| Connection retry with exponential backoff | Add after initial 1.0 feedback |

---

## Verification

```bash
# Run full test suite
pytest

# Type check core modules
mypy src/popoto/models/base.py src/popoto/models/query.py

# Lint
ruff src/ && black --check src/

# Verify version
grep -r "1.0.0" pyproject.toml
```

---

## Implementation Order

**Day 1 - Critical (Phase 1)**
1. Fix bare except clause
2. Add connection timeouts
3. Fix silent connection failure
4. Add thread safety docs

**Day 2 - High Priority (Phase 2)**
1. Export exceptions
2. Consolidate version to 1.0.0
3. Add type hints to core CRUD methods
4. Create CHANGELOG.md

**Day 3 - Polish & Release**
1. Phase 3 items (optional)
2. Full test run
3. Tag v1.0.0

---

## Success Criteria

- [ ] All Phase 1 items complete
- [ ] All Phase 2 items complete
- [ ] `pytest` passes (all 104+ tests)
- [ ] `mypy src/popoto/models/base.py` clean
- [ ] Version is 1.0.0 in single location
- [ ] CHANGELOG.md exists
- [ ] Exceptions importable from main namespace
